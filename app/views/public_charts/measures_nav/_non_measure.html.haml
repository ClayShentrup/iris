%li.parent.horizontal_padding_small
  = link_to(node.parent_title, back_button_options(node))
  %ul.unstyled
    %li.current_node
      %span.current_node_text.measures_nav_btn= node.title
      %ul.unstyled
        = render 'public_charts/measures_nav/child_nodes',
          node: node,
          current_node: node
