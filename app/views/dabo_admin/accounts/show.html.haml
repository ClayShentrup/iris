- # name, provider id, city and state
%ul
  %li
    Name:
    = @account.name
  %li
    Type:
    = @account.virtual_system_type
  %li
    Default Hospital:
    = @account.default_hospital_name
  %li
    Users:
    %ul
      - @account.users.each do |user|
        %li
          = user.email

= link_to 'Delete this account', |
  dabo_admin_account_path(@account), |
  method: :delete, |
  data: { confirm: 'Are you sure?' }

%br
= link_to 'Back', dabo_admin_accounts_path
