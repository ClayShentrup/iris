%li.category.bottom_margin_small.top_buffer_base
  %span.float_left.icon.icon_size_base.icon_left
    = render 'shared/icon', icon: '#icon_graph_bar'
  = highlight(node_link(bundle), term)
  %ul.measures.unstyled
    = render partial: 'child',
             as: :child,
             collection: bundle.children,
             locals: { term: term }
