!!!
%html
  %head
    %title Dabo Health
    %meta{content: 'width=device-width, initial-scale=1', name: 'viewport'}
    = stylesheet_link_tag    'application', media: 'all',
      'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    - if Rails.application.config.turbolinks_debugging_enabled
      = javascript_include_tag 'turbolinks_debugging', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body#body{ class: [controller_name, controller_action_name], |
         data: { |
           view_name: controller_action_name, |
           pixel_path: asset_path('pixel.gif'), |
           current_user_id: current_user.try(:id) |
         } |
       } |
    = render 'layouts/top_nav'
    - if feature? :feedback_bar
      = render 'layouts/feedback_bar'
    #outer_container
      = yield

    / This log out link is for engineering. Please do not remove.
    %ul
      - if user_signed_in?
        %li= current_user.email
    = link_to "Sign out", destroy_user_session_path, method: :delete

